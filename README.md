# SPI-CORE
This project implements a Serial Peripheral Interface (SPI) Controller using Verilog RTL. The design focuses on implementing the SPI master logic, handling serial data transmission and reception with register configurable clocking and data order options.
